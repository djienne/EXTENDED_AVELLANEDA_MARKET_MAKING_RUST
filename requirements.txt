# Extended DEX Python Dependencies
# Required for order signing via scripts/sign_order.py

# Core dependencies
aiohttp>=3.10.11
eth-account>=0.12.0
fast-stark-crypto==0.3.8
pydantic>=2.9.0
pyyaml>=6.0.1
sortedcontainers>=2.4.0
strenum>=0.4.15
tenacity>=9.1.2
websockets>=12.0,<14.0

# Development dependencies (optional, for testing)
python-dotenv==1.0.1
pytest==7.4.3
pytest-asyncio==0.23.3

# Note: The x10 module is provided by the local python_sdk-starknet directory
# To install the x10 SDK, run:
#   cd python_sdk-starknet && pip install -e .
